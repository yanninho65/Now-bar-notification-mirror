<?xml version="1.0" encoding="utf-8"?>
<!--
    4x1 widget, no background by design (Yann wants it to sit directly over the lock screen
    wallpaper via LockStar, not on a card).

    RESTRUCTURED to add the Sofascore "Sport" view (widget_sofascore_content below): the root is
    now a horizontal row — a persistent LEFT COLUMN (widget_app_icon + widget_view_toggle) that
    stays in the same place across both views, next to a CONTENT AREA (widget_content_area, a
    FrameLayout) that shows exactly one of two overlapping blocks at a time, switched by
    NowBarWidgetProvider.buildViewsUnsafe() based on WidgetViewModePrefs:
    - widget_latest_content: the ORIGINAL single-notification view — unchanged below except that
      widget_app_icon moved out into the shared left column, and the "tap to open" click target
      moved from the old whole-widget root onto this block specifically (see
      NowBarWidgetProvider.applyLatestContent). A widget instance with no Sofascore data at all
      still renders pixel-identical to before this change (toggle hidden, only this block shown).
    - widget_sofascore_content: up to 5 Sofascore matches side by side (widget_match_1..5, a
      fixed-SLOT-COUNT pattern like widget_action_1..3 above — but, since 17/09/2026, no longer a
      fixed-WIDTH one, see that block's own doc comment below), or widget_sofascore_empty if
      there's currently nothing to show. (5th slot added 17/09/2026 at Yann's request — was 4.)

    widget_view_toggle (the small rotating-arrows button under the icon) switches between LATEST
    and whichever of the two other views was shown last — see
    NowBarWidgetProvider.applyLeftToggle / onReceive(ACTION_TOGGLE_VIEW). It only shows in the
    latest-notif view once there's at least one Sofascore match OR "toutes notifs" entry to switch
    to; it always shows in the other two views so Yann can always get back.

    EXTENDED 17/09/2026 (Yann: "Sur la droite, ajouter un bouton pour changer vue entre sport et
    toutes notifs") with a THIRD overlapping block, widget_all_notifs_content — up to 5 recently
    RECEIVED notifications (not necessarily still active, see WidgetAllNotificationsStore), each
    tile either the generic image+title style or, for a Sofascore notification, today's exact
    match-tile style (widget_notif_N_match_image/score/period, structurally identical to
    widget_match_N above) — see NowBarWidgetProvider.applyAllNotifs. Reached via a NEW second
    toggle button, widget_view_toggle_right, at the far right edge of the widget (own column,
    symmetric to the left one but icon-less) — it flips directly between SPORT and ALL_NOTIFS only
    (see WidgetViewModePrefs), and stays hidden while LATEST is showing.

    FIXED 17/09/2026 (Yann: "L'augmentation de la taille des logos a fait que la cinquième notif
    est trop sur le côté. Réduire espace pour que ça passe avec 5. Rendre les espaces dynamiques
    si besoin en fonction de la taille du widget.") — both 5-tile rows (widget_sofascore_content
    and widget_all_notifs_content) used to give every tile a hard-coded 62dp width plus a 6dp
    android:layout_marginEnd gap, sized for an assumed widget width. When the logos grew from
    20dp to 30dp tall the same day, nothing shrank to compensate, so on a lock-widget host that
    doesn't give the widget quite that much room the 5th tile spilled past the edge instead of
    just being drawn smaller. Both rows are now WIDTH-RESPONSIVE instead: the row itself is
    android:layout_width="match_parent" (so it always claims exactly whatever width
    widget_content_area actually has right now — that FrameLayout is itself weighted to fill the
    remaining space in widget_root next to the fixed-width icon column and, when visible, the
    right toggle button, so this is always the real available width, not a guess), each of the 5
    tile columns is android:layout_width="0dp" with android:layout_weight="1" instead of a fixed
    62dp, and the fixed 6dp gaps are now thin android:layout_weight="0.1" spacer Views between the
    tiles instead of a marginEnd on each — so both the tiles AND the gaps between them shrink or
    grow together with the widget's actual width, which is the "espaces dynamiques" Yann asked
    for. The row declares an explicit android:weightSum="5.4" (5 tiles x weight 1 + 4 gaps x
    weight 0.1) precisely so this doesn't misbehave when there are FEWER than 5 entries to show:
    without an explicit weightSum, a LinearLayout only sums the weights of its currently VISIBLE
    children, so the 3 tiles left GONE when there are only 2 matches/notifications would have had
    their weight silently redistributed onto the 2 visible ones, stretching them to roughly double
    their normal size. With weightSum fixed at the FULL 5.4 regardless of how many tiles are
    GONE right now, each visible tile still only ever claims its own 1/5.4 share of the row, and a
    GONE tile just leaves its share of the row as blank trailing space — exactly the original
    look, just no longer hard-coded to one assumed widget width. Inside each tile, the elements
    that used to be pinned to a literal 62dp (the combined-logo/notification-photo ImageView, and,
    in "Toutes notifs", the image FrameLayout and the title TextView) now stretch to
    android:layout_width="match_parent" so they fill whatever width their own tile ends up with;
    the logo ImageView keeps android:adjustViewBounds="true" and android:scaleType="fitCenter" (and
    the android:maxWidth="62dp" that used to cap it is gone, since the tile itself is the cap now),
    so it still scales down to fit while keeping its aspect ratio, just against a flexible box
    instead of a fixed one.

    FIXED 18/09/2026 (Yann: "toujours aligner les icônes. Les aligner comme s'il y avait toujours 2
    lignes en dessous. Là, quand il y en a une seule ligne, l'image descend, elle ne devrait pas.")
    — widget_sofascore_content and widget_all_notifs_content both used android:gravity="center_vertical",
    so a tile whose own content was SHORTER than its tallest sibling (chiefly a "Toutes notifs"
    GENERIC tile whose title wraps to only 1 line instead of 2) got vertically centered within the
    row's full height — splitting the height difference evenly above AND below it, which pushed
    its image down relative to any neighbour with more text underneath. Both rows now use
    android:gravity="top" instead, so every tile's image sits flush with the top of the row
    regardless of how many lines its own text ends up using.

    FIXED 20/09/2026 (Yann: "entre vue icônes sport et toutes notifs, les textes ne sont pas sur la
    même ligne, mettre au même endroit que toutes notifs") — each widget_match_N_image used to be a
    bare 30dp-tall ImageView sitting directly above its score/period text, while the equivalent
    widget_notif_N image sits inside a fixed 32dp-tall widget_notif_N_image_area FrameLayout (the
    image itself still 30dp, just centered in those 32dp — see that block's own doc below). That
    2dp difference in image-area height meant the Sport view's score/period rows started 2dp
    higher than the identical-looking rows in "Toutes notifs", so the two views visually
    misaligned side by side despite otherwise-identical text sizes. Each widget_match_N_image is
    now wrapped in its own widget_match_N_image_area, matching widget_notif_N_image_area exactly
    (same 32dp height, same android:layout_gravity="center" on the image inside it), so both views
    now start their text at the exact same vertical offset.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@android:color/transparent"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!--
        Left column, shared by all three views — widget_app_icon shows the mirrored app's icon in
        the latest-notif view, Sofascore's own icon in the Sport view, or this app's own icon in
        the "toutes notifs" view (no single source app to show there — see
        NowBarWidgetProvider.applyAllNotifsIcon). widget_view_toggle sits right under it, GONE by
        default (same default-gone convention as widget_actions_container below), so nothing
        changes visually until there's actually another view to switch to.
    -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/widget_app_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:scaleType="centerCrop"
            android:contentDescription="@null" />

        <ImageView
            android:id="@+id/widget_view_toggle"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginTop="3dp"
            android:padding="3dp"
            android:background="@drawable/widget_dismiss_background"
            android:src="@drawable/ic_widget_switch_view"
            android:contentDescription="@null"
            android:visibility="gone" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/widget_content_area"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp">

        <!-- ORIGINAL single-notification view — unchanged below except widget_app_icon moving out above. -->
        <LinearLayout
            android:id="@+id/widget_latest_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <LinearLayout
                    android:id="@+id/widget_right_cluster"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerVertical="true"
                    android:layout_alignParentEnd="true"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <ImageView
                        android:id="@+id/widget_image"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_marginEnd="10dp"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <ImageView
                        android:id="@+id/widget_dismiss"
                        android:layout_width="22dp"
                        android:layout_height="22dp"
                        android:padding="4dp"
                        android:background="@drawable/widget_dismiss_background"
                        android:src="@drawable/ic_widget_dismiss"
                        android:contentDescription="@null" />

                </LinearLayout>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_centerVertical="true"
                    android:layout_toStartOf="@id/widget_right_cluster"
                    android:layout_alignParentStart="true"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/widget_title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:ellipsize="end"
                        android:textColor="@android:color/white"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        android:text="Titre de la notification" />

                    <TextView
                        android:id="@+id/widget_text"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="2"
                        android:ellipsize="end"
                        android:textColor="@android:color/white"
                        android:textSize="12sp"
                        android:text="Aperçu du texte de la notification, qui peut être un peu plus long que le titre." />
                </LinearLayout>

            </RelativeLayout>

            <LinearLayout
                android:id="@+id/widget_actions_container"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:visibility="gone">

                <TextView
                    android:id="@+id/widget_action_1"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="6dp"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:textColor="@android:color/white"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/widget_action_2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="6dp"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:textColor="@android:color/white"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/widget_action_3"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingStart="10dp"
                    android:paddingEnd="10dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:textColor="@android:color/white"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:visibility="gone" />

            </LinearLayout>

        </LinearLayout>

        <!--
            Sofascore "Sport" view. Up to 5 fixed match SLOTS (same fixed-slot-COUNT pattern as
            widget_action_1..3 above, rather than a RemoteViews collection, since the cap is a
            small fixed number) — see NowBarWidgetProvider.applySofascoreMatches. Each slot is a
            small vertical card: combined team image on top (Sofascore's own notification image —
            NOT cropped to a circle like widget_image above, since it's usually two logos side by
            side and a circular crop would cut one of them off), score (bracket around whichever
            side just scored, same convention as the Sofascore notification itself) in the middle,
            period/status at the bottom. Same top-to-bottom order as the watch's SMALL_IMAGE
            complication (wear/ComplicationImageComposer.kt), reworked to compact phone
            proportions rather than copying its pixel sizes (tuned for a 320px circular bitmap).

            Logo 30dp tall (grown from 20dp on 17/09/2026 at Yann's request: "Pour la vue sport,
            grossir les logos"). The Sofascore-match tiles inside "Toutes notifs" below
            (widget_notif_N_match_image) grew to this same 30dp height the same day, once Yann
            clarified that an appearance change to a Sofascore tile here should always carry over
            there too — see that view's own doc comment below for the corrected reasoning.

            Tile WIDTH is no longer a fixed 62dp (FIXED same day, see this file's class doc above
            for the full reasoning): each of the 5 tiles below is android:layout_width="0dp" +
            android:layout_weight="1", the row itself is match_parent, and a thin weight="0.1"
            spacer View sits between consecutive tiles instead of a fixed 6dp
            android:layout_marginEnd — so 5 tiles always fit inside whatever width the widget
            instance actually has (they used to keep the same 62dp width regardless, and a host
            giving the widget less room than that let the 5th tile spill off to the side once the
            logos' extra height widened the combined-logo images too). widget_sofascore_content's
            android:weightSum="5.4" (5 tiles x 1 + 4 gaps x 0.1) is what keeps a visible tile from
            stretching to fill the space of a currently-GONE one when there are fewer than 5
            matches — see the class doc above.
        -->
        <LinearLayout
            android:id="@+id/widget_sofascore_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="top"
            android:weightSum="5.4"
            android:visibility="gone">

            <LinearLayout
                android:id="@+id/widget_match_1"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_match_1_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_match_1_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_match_1_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_match_1_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_match_2"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_match_2_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_match_2_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_match_2_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_match_2_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_match_3"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_match_3_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_match_3_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_match_3_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_match_3_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_match_4"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_match_4_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_match_4_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_match_4_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_match_4_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <!-- 5th slot, added 17/09/2026 at Yann's request — exact copy of widget_match_1..4 above. -->
            <LinearLayout
                android:id="@+id/widget_match_5"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_match_5_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_match_5_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_match_5_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_match_5_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <TextView
                android:id="@+id/widget_sofascore_empty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/sofascore_widget_empty"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                android:visibility="gone" />

        </LinearLayout>

        <!--
            NEW (17/09/2026) — "Toutes notifs" view. Same fixed-slot-COUNT pattern as
            widget_sofascore_content above ("Toutes notifs : resemble au widget sport"), up to 5
            slots (widget_notif_1..5), and — since the same-day width fix described in this file's
            class doc above — the SAME width-responsive tiles/gaps too (0dp+weight="1" tiles, a
            weight="0.1" spacer View between each, match_parent row, weightSum="5.4"), rather than
            each tile's old fixed 62dp width. EACH slot can render in either of two styles,
            switched per-entry by NowBarWidgetProvider.applyAllNotifs depending on
            WidgetAllNotificationsStore.Data.kind — both declared statically (same fixed-slot
            philosophy as the rest of this file, see widget_match_N's class doc, rather than
            RemoteViews.addView with separate sub-layouts) and toggled with GONE/VISIBLE:
            - GENERIC (any mirrored app's notification): widget_notif_N_photo (the notification's
              own image, 32dp centerCrop circular — same treatment/size as widget_image above,
              UNCHANGED by the logo-size correction below since it isn't a Sofascore logo) with
              widget_notif_N_badge (the source app's icon, small, bottom-end corner — "en petit
              dans l'angle de l'image, l'icône [de l'app]") layered on top via the shared
              widget_notif_N_image_area FrameLayout (now match_parent-wide, following its tile).
              If the notification has NO image, widget_notif_N_photo shows the app's icon directly
              instead (full size, no badge — "si pas d'image [...] mettre juste icône appli à la
              place de l'image") and the badge stays hidden. Title below, up to 2 lines, width now
              matches the tile (match_parent, was a fixed 62dp) + ellipsize so a long title is
              truncated rather than ever bleeding into the next tile.
            - SOFASCORE_MATCH: widget_notif_N_match_image/score/period, STRUCTURALLY IDENTICAL to
              widget_match_N_image/score/period above (same ids pattern, and — CORRECTED
              17/09/2026 — now the SAME 30dp logo height again too) — "Garder la même présentation
              qu'aujourd'hui pour les notifs de Sofascore". A separate declaration rather than the
              literal same views, since widget_match_N's views can't be moved into a shared slot
              without also being in this FrameLayout at the same time as the Sport view. Briefly
              kept at the smaller original 20dp size right after widget_match_N grew (same day) on
              the assumption that "grossir les logos" only meant the dedicated Sport view — Yann
              corrected that same-day: any appearance change to a Sofascore match tile in the
              Sport view should carry over to its Sofascore tiles here too, so the two stay
              visually aligned (this is now a standing rule for this project, not a one-off — see
              the project's own working notes). The GENERIC content above (photo/title) simply
              shares whatever width the shared tile ends up with, no functional change there.
            widget_notif_N_image_area is a fixed 32dp-tall box regardless of which style is
            showing (the match image is 30dp tall, vertically centered within it) so every tile's
            text row lines up at the same height whether its neighbour is a photo or a match tile.
            Tapping a tile only opens the notification (contentIntent) — it is never wired to any
            dismiss/cancel action, so it never removes the notification (see
            NowBarWidgetProvider.applyAllNotifs).
        -->
        <LinearLayout
            android:id="@+id/widget_all_notifs_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="top"
            android:weightSum="5.4"
            android:visibility="gone">

            <LinearLayout
                android:id="@+id/widget_notif_1"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_notif_1_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_notif_1_photo"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_gravity="center"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <ImageView
                        android:id="@+id/widget_notif_1_match_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                    <ImageView
                        android:id="@+id/widget_notif_1_badge"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_gravity="bottom|end"
                        android:padding="1dp"
                        android:background="@drawable/widget_dismiss_background"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_notif_1_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:gravity="center_horizontal"
                    android:textColor="@android:color/white"
                    android:textSize="9sp"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_1_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_1_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_notif_2"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_notif_2_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_notif_2_photo"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_gravity="center"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <ImageView
                        android:id="@+id/widget_notif_2_match_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                    <ImageView
                        android:id="@+id/widget_notif_2_badge"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_gravity="bottom|end"
                        android:padding="1dp"
                        android:background="@drawable/widget_dismiss_background"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_notif_2_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:gravity="center_horizontal"
                    android:textColor="@android:color/white"
                    android:textSize="9sp"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_2_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_2_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_notif_3"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_notif_3_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_notif_3_photo"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_gravity="center"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <ImageView
                        android:id="@+id/widget_notif_3_match_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                    <ImageView
                        android:id="@+id/widget_notif_3_badge"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_gravity="bottom|end"
                        android:padding="1dp"
                        android:background="@drawable/widget_dismiss_background"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_notif_3_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:gravity="center_horizontal"
                    android:textColor="@android:color/white"
                    android:textSize="9sp"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_3_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_3_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_notif_4"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_notif_4_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_notif_4_photo"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_gravity="center"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <ImageView
                        android:id="@+id/widget_notif_4_match_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                    <ImageView
                        android:id="@+id/widget_notif_4_badge"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_gravity="bottom|end"
                        android:padding="1dp"
                        android:background="@drawable/widget_dismiss_background"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_notif_4_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:gravity="center_horizontal"
                    android:textColor="@android:color/white"
                    android:textSize="9sp"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_4_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_4_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!--
                Spacer between two tiles — a plain android.view.View here made the widget fail to
                add at all ("Impossible d'ajouter le widget", 17/09/2026): RemoteViews only
                inflates a fixed allowlist of view classes in the host process (launcher/LockStar)
                for security reasons, and plain View isn't on it (FrameLayout/LinearLayout/
                ImageView/TextView/etc. are, which is why those are safe everywhere else in this
                file) — an unsupported class throws at apply() time on the host side, which is
                exactly what surfaces as that generic "couldn't add" toast rather than any crash
                log this app itself could catch. An empty FrameLayout is the smallest supported
                stand-in with the exact same job: pure layout-weight space, no visible content.
            -->
            <FrameLayout
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="0.1" />

            <LinearLayout
                android:id="@+id/widget_notif_5"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal"
                android:visibility="gone">

                <FrameLayout
                    android:id="@+id/widget_notif_5_image_area"
                    android:layout_width="match_parent"
                    android:layout_height="32dp">

                    <ImageView
                        android:id="@+id/widget_notif_5_photo"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:layout_gravity="center"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null" />

                    <ImageView
                        android:id="@+id/widget_notif_5_match_image"
                        android:layout_width="match_parent"
                        android:layout_height="30dp"
                        android:layout_gravity="center"
                        android:adjustViewBounds="true"
                        android:scaleType="fitCenter"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                    <ImageView
                        android:id="@+id/widget_notif_5_badge"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_gravity="bottom|end"
                        android:padding="1dp"
                        android:background="@drawable/widget_dismiss_background"
                        android:scaleType="centerCrop"
                        android:contentDescription="@null"
                        android:visibility="gone" />

                </FrameLayout>

                <TextView
                    android:id="@+id/widget_notif_5_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:gravity="center_horizontal"
                    android:textColor="@android:color/white"
                    android:textSize="9sp"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_5_score"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="1dp"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="11sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_notif_5_period"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/white"
                    android:textSize="8sp"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <TextView
                android:id="@+id/widget_all_notifs_empty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_empty_title"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                android:visibility="gone" />

        </LinearLayout>

    </FrameLayout>

    <!--
        NEW (17/09/2026) — right-edge column, symmetric to the left one but icon-less: just the
        second toggle button, flipping directly between SPORT and ALL_NOTIFS (see
        WidgetViewModePrefs.toggleSportAllNotifs / NowBarWidgetProvider.applyRightToggle). GONE
        by default, same convention as widget_view_toggle — only shown while SPORT or ALL_NOTIFS
        is the current view.
    -->
    <!--
        FIXED 18/09/2026 (Yann: "sur la droite, supprimer les flèches et agrandir l'icône cloche
        et ballon") — grown from 20dp/3dp-padding (a ~14dp visible glyph) to 28dp/3dp-padding
        (~22dp visible) now that NowBarWidgetProvider.applyRightToggleIcon shows a single plain
        icon (bell or the football-pitch icon) instead of the old arrows+badge composite, which no
        longer needs the smaller, fiddlier size that composite was tuned for.
    -->
    <ImageView
        android:id="@+id/widget_view_toggle_right"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginStart="8dp"
        android:padding="3dp"
        android:background="@drawable/widget_dismiss_background"
        android:src="@drawable/ic_widget_switch_view"
        android:contentDescription="@null"
        android:visibility="gone" />

</LinearLayout>
