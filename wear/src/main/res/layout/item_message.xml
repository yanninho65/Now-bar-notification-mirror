<?xml version="1.0" encoding="utf-8"?>
<!-- REWORKED 25/09/2026 (Yann) — un message de la liste "Messages", même pilule que l'écran Sport
     (item_sport_match.xml) :
     - en haut à gauche l'image de la notif (sinon l'icône de l'appli), l'icône de l'appli en
       pastille en bas à droite de l'image (comme la miniature) ;
     - texte tronqué : "•••" bleu cliquable dessous pour tout afficher (MessagesActivity.makeExpandable) ;
     - le titre à droite de l'image ;
     - le texte / l'historique de la conversation dessous ;
     - les actions en boutons ronds sur une ligne (icônes, voir MessagesActivity.iconFor), plus
       "Aff. sur montre" / "Aff. sur tél." ; les libellés sans icône restent en pilule texte ;
     - le rond "supprimer sur le téléphone" en dessous des autres. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_notif_pill"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="12dp"
    android:paddingBottom="10dp"
    android:layout_marginBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <FrameLayout
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="8dp">

            <ImageView
                android:id="@+id/message_image"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:scaleType="centerCrop"
                android:contentDescription="@null" />

            <ImageView
                android:id="@+id/message_app_icon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="bottom|end"
                android:padding="1dp"
                android:background="@drawable/bg_app_badge"
                android:visibility="gone"
                android:contentDescription="@null" />
        </FrameLayout>

        <TextView
            android:id="@+id/message_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginTop="2dp"
            android:textColor="@color/detail_text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            android:includeFontPadding="false" />
    </LinearLayout>

    <TextView
        android:id="@+id/message_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:textColor="@color/detail_text_primary"
        android:textSize="14sp"
        android:maxLines="6"
        android:ellipsize="end"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/message_lines_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:orientation="vertical"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/message_text_actions_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="vertical"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/message_actions_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:gravity="center" />

    <ImageButton
        android:id="@+id/message_delete_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_delete_button"
        android:src="@drawable/ic_delete"
        android:scaleType="fitCenter"
        android:padding="9dp"
        android:contentDescription="@string/action_delete" />
</LinearLayout>
